# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Test
'on':
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -L \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            curl -L https://npm.pkg.github.com/@entur/enki